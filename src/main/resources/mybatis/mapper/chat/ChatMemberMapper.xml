<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yoim.www.mapper.chat.ChatMemberMapper">

	<resultMap type="chatMember" id="getSelectMap">
		<result property="roomId" column="room_id" />
		<result property="userId" column="user_id" />
		<result property="role" column="role" />
		<result property="joinedAt" column="joined_at" />
		<result property="leftAt" column="left_at" />
		<result property="lastReadSeq" column="last_read_seq" />
		<result property="notificationsOn" column="notifications_on" />
		<result property="mutedUntil" column="muted_until" />
	</resultMap>

	<insert id="insertMember">
		INSERT IGNORE INTO tb_chat_member(room_id,user_id,role)
		VALUES(#{roomId},#{userId},#{role})
	</insert>
	<update id="updateLastReadSeq">
		UPDATE tb_chat_member SET last_read_seq = GREATEST(last_read_seq, #{seq})
		WHERE room_id=#{roomId} AND user_id=#{userId}
	</update>

	<select id="selectMemberReadStates" resultMap="getSelectMap">
		SELECT
			user_id AS userId,
			COALESCE(last_read_seq, 0) AS last_read_seq
		FROM tb_chat_member
		WHERE room_id = #{roomId}
	</select>


	<!-- 메시지 읽음 카운트 -->
	<select id="readCountForMessage" resultType="int">
		SELECT SUM(CASE
		WHEN cm.user_id <![CDATA[ <> ]]> #{senderId}
		AND cm.joined_at <![CDATA[ <= ]]> #{createdAt}
		AND (cm.left_at IS NULL OR cm.left_at <![CDATA[ >= ]]> #{createdAt})
		AND cm.last_read_seq <![CDATA[ >= ]]> #{seq}
		THEN 1 ELSE 0 END) AS read_count
		FROM tb_chat_member cm
		WHERE cm.room_id = #{roomId}
	</select>


</mapper>
